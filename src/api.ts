/**
 * -------------------------------------------------------------
 * DevDash - API Modules (Day 1 Placeholder)
 * -------------------------------------------------------------
 */

export async function fetchJson<T>(url: string): Promise<T> {
  // To be fully implemented in Day 2
  console.log(`API call placeholder requested for URL: ${url}`);
  return {} as T;
}
